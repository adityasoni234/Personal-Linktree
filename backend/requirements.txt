# ---- Web framework -------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ---- Validation / settings ----------------------------------------------
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.2.0

# ---- Database ------------------------------------------------------------
SQLAlchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1

# ---- Cache / rate limiting ----------------------------------------------
redis==5.2.1

# ---- Security ------------------------------------------------------------
PyJWT==2.10.1
argon2-cffi==23.1.0
defusedxml==0.7.1

# ---- QR + imaging --------------------------------------------------------
qrcode==8.0
Pillow==11.1.0
# Rasterises sanitised SVG logos into PNG exports. Needs libcairo2 at runtime;
# the QR engine degrades gracefully (SVG-only logos) if it is unavailable.
CairoSVG==2.7.1

# ---- Object storage (optional backend) ----------------------------------
boto3==1.35.90
